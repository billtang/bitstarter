
   sudo apt-get update
   sudo apt-get install python-software-properties python g++ make 
   sudo apt-get-respository ppa:chris-lea/node.js
   sudo aadd-get-respository ppa:chris-lea/node.js
   sudo add-get-respository ppa:chris-lea/node.js
   sudo add-apt-respository ppa:chris-lea/node.js
   sudo add-apt-repository ppa:chris-lea/node.js
   sudo apt-get update
   sudo apt-get install nodejs

   sudo apt-get install -y git-core
   wget -qO-  https://toolbelt.heroku.com/install-ubuntu.sh |sh
   which git
   which heroku

   heroku login

   ls -al .ssh/
   ssh-keygen -t rsa
   heroku keys:add
   
npm install nodeunit

git clone https://github.com/danwrong/restler


locally run this:
node web.js

then access http://192.168.5.128:5000